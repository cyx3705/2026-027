# 验证合同

- `VERIFY-PORT-001`：以 5.1.0 发布快照执行锁定还原、Release 构建和 Contracts 测试，结果必须为零警告、零错误。
- `VERIFY-PORT-002`：Contracts 覆盖回环 Web 持券认证、MCP 本机不持券、Cursor mcp.json 去鉴权头对齐、离线 CLI 不抢航线、敏感参数审计脱敏、热重载端口语义及命令注册表投影。
- `VERIFY-PORT-003`：候选包必须含 module manifest、模块 DLL、XML 文档和无 BOM 的 `SHA256SUMS`，不得包含任何 `HistoryVulcan.*.dll`。

## 1.0.6 参数保真回归（2026-09-13）

- VERIFY-PORT-005：JsonStringArgumentsReachHandlerUnchanged 覆盖中文、字面量 Unicode 转义、引号/反斜杠/换行/制表/emoji、空串和含等号文本。Contracts 36/36 通过。
- 根因：BuildCommandText 使用 JSON 字符串编码，但命令解析器只解释引号和反斜杠引用，导致 Unicode 与控制字符成为转义文本。改用宿主 QuoteArg。真实 MCP 验证在候选热重载后执行。
