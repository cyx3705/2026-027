# HistoryPortunus 项目概览

更新日期：2026-08-25

HistoryPortunus 1.0 是 HistoryVulcan 的对外传输模块，向本机前端提供回环 Web 接口，并向 agent 提供 MCP 接口。它以 HistoryVulcan 5.1.0 及以上的已发布快照为唯一宿主基线。

模块只依赖宿主 Core 中的命令总线和命令注册器。MCP schema、外部调用可见性、会话、审计与传输设置由模块自身维护；包不包含任何 `HistoryVulcan.*.dll`。
